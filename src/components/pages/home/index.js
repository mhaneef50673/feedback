import React from 'react';
import Feedback from '../../organisms/feedback';

/**
 * Renders when any url matches route /
 */
const Home = props => {
  return (
    <Feedback />
  );
};

export default Home;
